apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: postgresql-llm
    type: postgres
    url: postgres:5432
    user: grafana_reader
    secureJsonData:
      password: grafana_readonly
    jsonData:
      database: llm_observability
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 2
      connMaxLifetime: 14400
      postgresVersion: 1500
      timescaledb: false
    isDefault: true
    editable: false
